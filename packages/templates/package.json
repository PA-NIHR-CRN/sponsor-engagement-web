{
  "name": "@nihr-ui/templates",
  "version": "0.0.0",
  "license": "MIT",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "lint": "eslint .",
    "precompile": "ts-node ./scripts/precompile-templates.ts",
    "test": "jest --watch",
    "test:ci": "jest --ci --coverage"
  },
  "dependencies": {
    "@nihr-ui/logger": "*",
    "handlebars": "^4.7.8"
  }
}

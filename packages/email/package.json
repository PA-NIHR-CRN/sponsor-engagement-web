{
  "name": "@nihr-ui/email",
  "version": "0.0.0",
  "license": "MIT",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "lint": "eslint .",
    "test": "jest --watch",
    "test:ci": "jest --ci --coverage"
  },
  "dependencies": {
    "@nihr-ui/logger": "*",
    "async": "^3.2.5",
    "aws-sdk": "^2.1488.0",
    "pino": "^8.14.1",
    "pino-pretty": "^10.0.1"
  },
  "devDependencies": {
    "@types/async": "^3.2.24"
  }
}

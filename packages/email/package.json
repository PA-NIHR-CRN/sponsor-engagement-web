{
  "name": "@nihr-ui/email",
  "version": "0.0.0",
  "license": "MIT",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "lint": "eslint .",
    "test": "jest --watch",
    "test:ci": "jest --ci --coverage"
  },
  "dependencies": {
    "@nihr-ui/logger": "*",
    "aws-sdk": "^2.1488.0",
    "bottleneck": "^2.19.5",
    "pino": "^8.14.1",
    "pino-pretty": "^10.0.1"
  }
}

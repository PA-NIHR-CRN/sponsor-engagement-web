{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "db:generate"],
      "outputs": ["dist/**/*", ".next/**", "!.next/cache/**"],
      "env": ["NODE_ENV", "NEXTAUTH_URL", "AUTH_CLIENT_ID", "AUTH_CLIENT_SECRET", "AUTH_WELL_KNOWN_URL"]
    },
    "start": {},
    "lint": {
      "dependsOn": ["db:generate"]
    },
    "lint:fix": {
      "dependsOn": ["db:generate"]
    },
    "test": {
      "cache": false,
      "dependsOn": ["db:generate"],
      "dotEnv": [".env.local"],
      "env": ["NODE_ENV", "NEXTAUTH_URL", "AUTH_CLIENT_ID", "AUTH_CLIENT_SECRET", "AUTH_WELL_KNOWN_URL"]
    },
    "test:ci": {
      "dependsOn": ["db:generate"],
      "outputs": ["coverage/**"]
    },
    "typecheck": {},
    "dev": {
      "cache": false,
      "dependsOn": ["db:generate"],
      "persistent": true,
      "env": ["NODE_ENV", "NEXTAUTH_URL", "AUTH_CLIENT_ID", "AUTH_CLIENT_SECRET", "AUTH_WELL_KNOWN_URL"]
    },
    "db:generate": { "cache": false },
    "db:push": {}
  }
}

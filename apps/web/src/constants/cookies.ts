/**
 * Name of cookie which stores whether user accepted/rejected additional cookies
 */
export const SE_GDPR_COOKIE_NAME = 'SEConsentGDPR'

/**
 * Value of GDPR consent cookie when user has accepted additional cookies
 */
export const SE_GDPR_COOKIE_ACCEPT_VALUE = 'Accept'

/**
 * Value of GDPR consent cookie when user has rejected additional cookies
 */
export const SE_GDPR_COOKIE_REJECT_VALUE = 'Reject'

/**
 * Number of months until GDPR consent cookie expires
 */
export const SE_GDPR_COOKIE_EXPIRY_MONTHS = 12

/**
 * Name of custom event for when a user changes their cookie settings
 */
export const COOKIE_SETTINGS_CHANGE_EVENT = 'cookieSettingsChanged'
